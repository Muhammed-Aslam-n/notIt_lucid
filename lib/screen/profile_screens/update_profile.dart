import 'package:flutter/material.dart';

class ScreenUpdateProfile extends StatelessWidget {
  const ScreenUpdateProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
